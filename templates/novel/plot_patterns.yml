plot_patterns:
  - id: betrayal
    name: Betrayal
    description: Trust is broken for a hidden gain.
    roles:
      - id: betrayer
        description: The one who betrays.
        required: true
      - id: victim
        description: The one betrayed.
        required: true
    required_beats:
      - type: setup
        description: The betrayer earns trust.
      - type: payoff
        description: The betrayal is revealed.
