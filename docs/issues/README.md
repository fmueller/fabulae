# Legacy Issues (Archived)

> Issues have been migrated to `backlog/tasks/`. Use `backlog board` to view the current task board.

The files in this directory are kept for reference. All build improvement issues from `build-improvements.md` have been migrated as individual backlog tasks (TASK-5 through TASK-14).
