# Legacy Tasks (Archived)

> Tasks have moved to `backlog/tasks/`. Use `backlog board` to view the current task board.

The task files in this directory are historical records of completed work. For pending and active tasks, see the [backlog](../backlog/tasks/).
